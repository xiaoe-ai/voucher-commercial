name: Commercial Canonical Business E2E

on:
  workflow_dispatch:
  push:
    paths:
      - 'supabase/canonical/**'
      - '.github/workflows/commercial-canonical-e2e.yml'

permissions:
  contents: read

jobs:
  business-e2e:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      PGHOST: 127.0.0.1
      PGPORT: '5432'
      PGDATABASE: postgres
      PGUSER: postgres
      PGPASSWORD: postgres

    steps:
      - uses: actions/checkout@v4

      - name: Bootstrap Supabase auth shim
        shell: bash
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 <<'SQL'
          do $$
          begin
            if not exists (select 1 from pg_roles where rolname='anon') then create role anon nologin; end if;
            if not exists (select 1 from pg_roles where rolname='authenticated') then create role authenticated nologin; end if;
            if not exists (select 1 from pg_roles where rolname='service_role') then create role service_role nologin bypassrls; end if;
          end $$;
          create schema if not exists auth;
          create table auth.users(id uuid primary key, email text, created_at timestamptz not null default now());
          create or replace function auth.uid() returns uuid language sql stable as $$
            select nullif(current_setting('request.jwt.claim.sub', true),'')::uuid
          $$;
          create or replace function auth.jwt() returns jsonb language sql stable as $$
            select coalesce(nullif(current_setting('request.jwt.claims', true),'')::jsonb,'{}'::jsonb)
          $$;
          grant usage on schema auth to anon, authenticated, service_role;
          grant execute on function auth.uid() to anon, authenticated, service_role;
          grant execute on function auth.jwt() to anon, authenticated, service_role;
          grant select on auth.users to service_role;
          SQL

      - name: Apply canonical SQL
        shell: bash
        run: |
          set -euo pipefail
          for f in $(find supabase/canonical -maxdepth 1 -type f -name '*.sql' | sort); do
            psql -v ON_ERROR_STOP=1 -f "$f"
          done
          echo 'CANONICAL_SQL_OK=true'

      - name: Seed isolated Commercial fixtures
        shell: bash
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 <<'SQL'
          insert into auth.users(id,email) values
            ('aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa','admin@example.invalid'),
            ('bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb','partner@example.invalid'),
            ('dddddddd-dddd-4ddd-8ddd-dddddddddddd','staff@example.invalid');

          insert into public.partners(id,partner_code,partner_name,status,staff_limit,staff_access_enabled) values
            ('10000000-0000-4000-8000-000000000001','ADMIN','Commercial Voucher Admin','active',10,true),
            ('10000000-0000-4000-8000-000000000002','PA','Partner A','active',10,true);

          insert into public.partner_users(user_id,partner_id,role,status,login_email) values
            ('aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa','10000000-0000-4000-8000-000000000001','admin','active','admin@example.invalid'),
            ('bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb','10000000-0000-4000-8000-000000000002','partner_admin','active','partner@example.invalid');

          insert into public.branches(id,branch_code,branch_name,status) values
            ('20000000-0000-4000-8000-000000000001','BA','Branch A','active');

          insert into public.staff_users(user_id,branch_id,staff_name,role,status) values
            ('dddddddd-dddd-4ddd-8ddd-dddddddddddd','20000000-0000-4000-8000-000000000001','Staff A','staff','active');

          insert into public.voucher_templates(id,template_code,template_name,voucher_category,status) values
            ('30000000-0000-4000-8000-000000000001','WELCOME','Welcome Voucher','custom','active');

          insert into public.voucher_versions(
            id,template_id,version_no,version_name,validity_type,valid_days,status,all_branches,usage_limit
          ) values (
            '40000000-0000-4000-8000-000000000001',
            '30000000-0000-4000-8000-000000000001',1,'V1','days_after_issue',30,'active',true,1
          );

          update public.voucher_templates
          set current_version_id='40000000-0000-4000-8000-000000000001'
          where id='30000000-0000-4000-8000-000000000001';

          insert into public.partner_voucher_access(partner_id,template_id,status,quota_type) values
            ('10000000-0000-4000-8000-000000000002','30000000-0000-4000-8000-000000000001','active','unlimited');

          insert into public.partner_claim_settings(partner_id,all_branches) values
            ('10000000-0000-4000-8000-000000000002',true);

          insert into public.partner_voucher_allocations(
            id,partner_id,version_id,quantity_allocated,quantity_revoked,status,all_branches
          ) values (
            '50000000-0000-4000-8000-000000000001',
            '10000000-0000-4000-8000-000000000002',
            '40000000-0000-4000-8000-000000000001',
            2,0,'active',true
          );
          SQL

      - name: Partner issues voucher
        shell: bash
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 <<'SQL'
          set role authenticated;
          select set_config('request.jwt.claim.sub','bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb',false);
          do $$
          declare r jsonb;
          begin
            r := public.create_partner_multi_voucher_controlled(
              '40000000-0000-4000-8000-000000000001'::uuid,
              'Recovery Test Customer',
              '+000000000'
            );
            if coalesce((r->>'success')::boolean,false) is not true then
              raise exception 'Issue failed: %', r;
            end if;
            if nullif(r->>'voucher_code','') is null then
              raise exception 'Voucher code missing: %', r;
            end if;
          end $$;
          reset role;
          SQL
          psql -Atqc "select voucher_code from public.vouchers where customer_name='Recovery Test Customer' order by issued_at desc limit 1" > /tmp/voucher_code
          test -s /tmp/voucher_code
          echo 'PARTNER_ISSUE_OK=true'

      - name: Staff verifies and redeems voucher
        shell: bash
        run: |
          set -euo pipefail
          CODE=$(cat /tmp/voucher_code)
          psql -v ON_ERROR_STOP=1 -v voucher_code="$CODE" <<'SQL'
          select set_config('app.test_voucher_code', :'voucher_code', false);
          set role authenticated;
          select set_config('request.jwt.claim.sub','dddddddd-dddd-4ddd-8ddd-dddddddddddd',false);
          do $$
          declare code text := current_setting('app.test_voucher_code'); v jsonb; r jsonb; r2 jsonb;
          begin
            v := public.verify_voucher(code);
            if coalesce((v->>'success')::boolean,false) is not true then
              raise exception 'Verify failed: %', v;
            end if;
            if coalesce((v->>'can_redeem')::boolean,false) is not true then
              raise exception 'Voucher not redeemable: %', v;
            end if;

            r := public.redeem_voucher(code,'isolated recovery e2e');
            if coalesce((r->>'success')::boolean,false) is not true then
              raise exception 'Redeem failed: %', r;
            end if;

            r2 := public.redeem_voucher(code,'second attempt');
            if coalesce((r2->>'success')::boolean,false) is true then
              raise exception 'Second redemption unexpectedly succeeded: %', r2;
            end if;
          end $$;
          reset role;
          SQL

          psql -v ON_ERROR_STOP=1 <<'SQL'
          do $$ begin
            if (select count(*) from public.redemptions where status='success') <> 1 then
              raise exception 'Expected exactly one successful redemption';
            end if;
            if not exists(select 1 from public.vouchers where customer_name='Recovery Test Customer' and status='redeemed') then
              raise exception 'Voucher status did not become redeemed';
            end if;
          end $$;
          SQL
          echo 'STAFF_VERIFY_REDEEM_OK=true'
          echo 'DOUBLE_REDEEM_BLOCKED=true'

      - name: Emit business E2E evidence
        shell: bash
        run: |
          echo 'COMMERCIAL_CANONICAL_BUSINESS_E2E_OK=true'
          echo 'FLOW=partner_issue->staff_verify->staff_redeem'
          echo 'PRODUCTION_TOUCHED=false'
